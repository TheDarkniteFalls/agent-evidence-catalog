# Second balanced expansion batch

Status: unpublished evidence-work selection, 2026-08-01. This is an ordering
for dossier work, not a product ranking, score, suitability judgment or
recommendation.

The accepted candidate registry remains unchanged. Current publisher sources
were refreshed before this selection.

## Selected surfaces

1. **Anthropic Claude Code CLI 2.1.117** — local CLI. The publisher has an
   immutable `v2.1.117` release and exact commit. This is the batch's only
   independent-evaluation admission attempt because the Overeager Coding
   Agents paper names that client version, three models and a deliberately
   permission-bypassed container configuration. The study still requires a
   separate independence, disclosure and public-artifact gate audit.
2. **Zed Agent in Zed 1.13.1** — desktop IDE. Zed's dated stable release and
   current documentation distinguish the native Zed Agent from External Agents
   and Terminal Threads, and separately document model-provider paths, agent
   profiles, tool permissions and OS-enforced sandboxing.
3. **Replit Agent 4 hosted workspace** — hosted service. Replit dates the Agent
   4 generation and publishes current Plan/Build approval, mode, model,
   background-task, browser-testing and workspace secret boundaries.
   The rolling backend and effective model revisions remain unresolved.

## Why this batch

The three records cover local CLI, desktop IDE and hosted-service delivery.
They add an exact evaluation-pinned client, a client release with several
mutually exclusive agent paths, and a named hosted generation whose service
continues to change. Claude is the only independent-evidence attempt. No
claim, alias or evaluation may transfer between these surfaces or from any
accepted related record.

## Refreshed primary anchors

- Claude Code `v2.1.117` release and commit, Claude Code overview, permissions,
  sandbox and model documentation.
- Zed 1.13.1 stable release, Zed Agent, Agent Profiles, Tool Permissions,
  Sandboxing, and AI Privacy documentation.
- Replit Agent 4 launch changelog, Agent Modes, Model Selector, Plan Mode, task
  system, App Testing and Secrets documentation.

Missing immutable runtime builds, selected configurations and provider
revisions are evidence gaps. They do not disqualify an identifiable surface.
