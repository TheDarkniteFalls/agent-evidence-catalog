# Replit Agent 4 hosted workspace dossier

Unpublished, public-source-only dossier for the Replit Agent hosted workspace
after the dated Agent 4 generation milestone.

The record treats `Agent 4` as a publisher-named release line, not an immutable
backend build. Current service, model, Plan/Build, execution-mode, task,
browser-testing and secret documentation is rolling-current and separately
scoped. Publisher-described App Testing remains self-testing, not independent
evaluation.

The canonical source is `dossier-source.json`. Its eight raw claim records are
stable source records under `claims/replit-agent-4-hosted/`. A generated record
may be added only after the source-only validator passes.

No Replit product was installed or run. No account, workspace, task, test,
secret, deployment or model route was opened. This record is unpublished,
not independently tested and supports no score, rank or recommendation.
