# Unpublished OpenHands CLI public-source dossier

Status: local research draft only. This dossier is outside all synthetic and
public build inputs.

The exact release anchor is **OpenHands CLI 1.16.0** at signed tag commit
`2963442dacc7cea44e39b7c4e73724295c853465`. Exact-version claims use the
release page or files at tag `1.16.0`. Live documentation claims remain
`rolling-current` and are not projected backward onto the release.

## Boundary

- OpenHands CLI was not downloaded, installed, run or independently tested.
- No account was created, no model was called and no publisher was contacted.
- The Python package and standalone binary delivery paths are documented, but
  no installed artifact or digest was selected.
- The dossier covers the OpenHands CLI product surface. OpenHands Cloud appears
  only where the CLI can delegate to it; Cloud is not treated as the same
  runtime.
- All behavior statements are publisher-attributed, not catalog observations.
- No score, ranking, recommendation, suitability result or publication action
  exists here.

## Reading order

1. `dossier-source.json` — authored dossier framing, configuration axes and
   machine-readable proposition/persona mappings.
2. `claims/` — stable raw claim records.
3. `SOURCE_NOTES.md` — source selection, capture and exclusion notes.
4. `../../records/org.openhands.cli-1-16-0.json` — deterministic generic
   real-agent record after the draft build runs.

