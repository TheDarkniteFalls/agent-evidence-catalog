# GitLab Duo Developer Flow 18.8 dossier

Status: unpublished public-source research draft. No GitLab product, runner,
agent, model or service was installed, invoked or independently tested.

This dossier pins the GitLab `v18.8.0-ee` source release and the GitLab 18.8
general-availability milestone for the Developer Flow. That anchor does not
freeze GitLab.com or Dedicated service revisions, the GitLab-managed flow
executor, `@gitlab/duo-cli`, the model backend, a CI/CD runner, or an effective
approval policy.

The record therefore keeps separate:

- GitLab Self-Managed source tag `v18.8.0-ee` and its source revision;
- GitLab.com, GitLab Self-Managed and GitLab Dedicated offering scope;
- the 18.8 GA milestone and rolling current documentation;
- human and service-account composite identity;
- issue, assignment, mention and later Agentic Chat invocation paths;
- GitLab-hosted and user-managed runners plus their executor and image choices;
- project `agent-config.yml`, setup, cache and network policy;
- GitLab-managed and self-hosted model configuration; and
- the unresolved 18.8 approval boundary versus later background-flow tool
  governance.

All claims remain attributed to GitLab, Inc. They are not catalog observations.
No suitability, score, ranking, recommendation or safety conclusion exists.

